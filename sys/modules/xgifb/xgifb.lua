require 'hw'
require 'core'

function xgifbMatch(parent, match, pa)
	 --core.print("matching\n")
	 return 0
end
function xgifbAttach(parent, sc, pa)
	 --core.print("attaching\n")
end

function onClose()
end
